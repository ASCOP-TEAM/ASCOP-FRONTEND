import styled from 'styled-components';

export const Section = styled.section`
  padding: 2rem 0;
  .list-products {
    display: flex;
    list-style: none;
    padding: 0;
    gap: 1rem;
    flex-direction: column;
  }

  .info-conatiner {
    position: sticky;
    bottom: 0;
  }
`;
